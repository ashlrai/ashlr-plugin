{
  "mcpServers": {
    "ashlr-efficiency": {
      "command": "bun",
      "args": ["run", "${CLAUDE_PLUGIN_ROOT}/servers/efficiency-server.ts"],
      "description": "Token-efficient Read/Grep/Edit + savings tracker"
    },
    "ashlr-sql": {
      "command": "bun",
      "args": ["run", "${CLAUDE_PLUGIN_ROOT}/servers/sql-server.ts"],
      "description": "Compact SQL execution for SQLite + Postgres (explain mode, schema introspection)"
    },
    "ashlr-bash": {
      "command": "bun",
      "args": ["run", "${CLAUDE_PLUGIN_ROOT}/servers/bash-server.ts"],
      "description": "Bash with auto-compressed stdout + structured summaries for common commands"
    }
  }
}
