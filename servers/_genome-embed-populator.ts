/**
 * Stub for _genome-embed-populator — populator not yet merged from its sprint
 * branch. This stub keeps the worktree compiling while the nudge-telemetry
 * work ships; delete once the real module lands on main.
 */

export async function populateGenomeEmbeddings(
  _genomeRoot: string,
  _opts: { ctxDb: unknown; projectHash: string },
): Promise<void> {
  return;
}
