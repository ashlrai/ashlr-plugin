node_modules/
dist/
.DS_Store
*.log
bun.lockb
bun.lock
.env
.env.*
.claude/
# SQLite runtime artifacts from local server dev (server/ashlr.db, -wal, -shm).
server/*.db
server/*.db-wal
server/*.db-shm
# Runtime-mutating genome state — written by genome-auto-propose hook and
# drained by the SessionEnd consolidator. Tracking these blocks /ashlr-update
# fast-forwards in plugin checkouts that have run at least once.
.ashlrcode/genome/proposals.jsonl
.ashlrcode/genome/knowledge/discoveries-auto.md
